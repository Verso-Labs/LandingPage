# Verso AI Landing Page

Static one-pager for [Verso AI](https://verso-ai.com) — professional presentations from prompts.

## Stack

Single `index.html` using Tailwind CSS via CDN. No build step.

## Deploy

Push to `main` and enable GitHub Pages (Settings > Pages > Source: `main` branch, root folder).

## Customize

- **Accent color**: Change `#4CAF50` in the `tailwind.config` block inside `index.html`
- **CTA links**: Search for `href="#"` and replace with real URLs
- **Content**: Edit text directly in the HTML sections
